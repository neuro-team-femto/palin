% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdt.R
\name{sdt_multinom_loss}
\alias{sdt_multinom_loss}
\title{Multinomial negative log-likelihood for the SDT model}
\usage{
sdt_multinom_loss(
  pars,
  data,
  method = c("expectation", "simulation"),
  ntrials = 10000,
  eps = 1e-12,
  response_levels = NULL,
  ...
)
}
\arguments{
\item{pars}{Numeric vector of length 2. First value is response bias, second
value is internal noise.}

\item{data}{Data frame. Must contain columns \code{block1} and \code{block2},
as returned by \code{sdt_data(..., return_summary = FALSE)}.}

\item{method}{Character. Either \code{"expectation"} or \code{"simulation"}.}

\item{ntrials}{Numeric. Number of trials used when \code{method = "simulation"}.}

\item{eps}{Numeric. Small positive value used to avoid probabilities of
exactly zero.}

\item{response_levels}{Optional length-2 vector specifying the observed
response labels corresponding to \code{stim1} and \code{stim2}. The first value is
mapped to \code{stim1}, and the second value is mapped to \code{stim2}. For example,
use \code{response_levels = c(0, 1)} when 0 means \code{stim1} and 1 means \code{stim2}.}

\item{...}{Additional arguments. Currently unused, but accepted for
compatibility with optimisation functions.}
}
\value{
Numeric value: the negative multinomial log-likelihood.
}
\description{
Computes the negative multinomial log-likelihood of the observed double-pass
response patterns under the SDT model.
}
